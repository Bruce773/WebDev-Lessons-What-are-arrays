// 1. Write a function called returnArray that that takes in two arguments and when invoked
// returns an array containing those arguments.


// 2. Write a function called returnItemAtArrayIndex that takes in one argument (a number), searches
// for that index on arr1, and returns the value at that index on the arr1 array.

// Example
// returnItemAtArrayIndex(1) -> would return '2'

var arr1 = ['1', '2', 3, ['innerArrayItem', [2, 3]], 'Last Item'];
